import chalk from "chalk";

export const STYLES = {
  user: chalk.cyan.bold,
  ai: chalk.green.bold,
  system: chalk.yellow.bold,
  error: chalk.red.bold,
  success: chalk.green,
  dim: chalk.dim,
  highlight: chalk.magenta.bold,
  border: chalk.gray,
  spinner: chalk.cyan,
  thinking: chalk.dim.italic,
  executing: chalk.yellow.bold,
  done: chalk.green.bold,
  default: chalk.blue,
};
